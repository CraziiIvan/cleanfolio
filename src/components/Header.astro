---
type Props = {
  title: string;
  description: string;
  date?: string
};

const { title, description, date } = Astro.props;
---

<header class="mb-8">
  <h1 class=" text-xl font-medium">{title}</h1>
  <span class=" text-zinc-600">{date ||description}</span>
</header>
